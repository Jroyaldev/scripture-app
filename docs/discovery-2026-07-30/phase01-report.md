# Phase 0+1 repair agent report (2026-07-30, commits 5a44c1b + 4a6a26c on codex/quire-redesign)

Both commits are in, gates green.

## Commit 1 — `5a44c1b` `fix: the strip keeps the rules the kicker took with it`
9 files, +478/−100.

- **(a) Tooltip drift** — `src/renderer/components/Tooltip.tsx`: `PAGE_TOP_EDGE` 54 → 40, prose corrected. `tests/quire-frame-top-edge-contract.test.ts` no longer pins a literal: a module-level `FRAME_TOP` is computed by adding the `--page-inset` and `--register-strip` declarations out of the sheets, and the assertion is built from it (`new RegExp(`const PAGE_TOP_EDGE = ${FRAME_TOP};`)`). The five placement cases are expressed relative to `FRAME_TOP` too. Moving the edge to 54 next phase is one edit in Tooltip.tsx, demanded by name.
- **(b) Saving live region** — `styles.css`: deleted `:not(.is-saving):not(.is-failed) { display: none }`; the sr-only clip is now keyed `:not(.is-failed)`, so the region is in the a11y tree at rest and while saving, `position: absolute` in both (zero layout shift), and `.is-failed` stays in flow and visible. The idle branch's inert `<span class="sr-only">Tabs saved</span>` is now `null` — at rest the region is empty so the settle is silent and a write announces once. New premium-contract test defends all of it (there was no test at all before).
- **(c) Dead flush-start** — `flushStart = activeRegisterIndex === 0`; `leadGroup`/`leadKickered` deleted. Contract restated to assert the live rule plus `doesNotMatch(/leadKickered|leadGroup/)`.
- **(d) Keyboard collapse parity** — removed the Enter/Space intercept in `handleTabKeyDown`. A button activates itself, so keyboard (and AT `element.click()`) now land in the one click handler that holds collapse / expand / select, and `event.detail === 0` finally discriminates. Delete/Backspace stay in keydown. New strip-interactions test replaces the old "Enter/Space are the only commit path" claim with a parity claim; the old assertion is quoted in the note. Announcement: the folded study's proxy *is* the pressed tab, so its accessible name changes under retained focus — no new ARIA invented.
- **(e) Plus geometry + copy** — `margin: 0 auto 3px 4px` → `2px`; verified in a real Chromium that the anchor's margin box is now exactly 30px with its top flush to the tab tops (was 31, overhanging 1px). Tooltip copy: "studies" → "tabs".
- **(f) QA gate + dead guard** — dropped `[data-study-group-tab]` from the viewport context-menu guard. `npm run qa:study-workspace-bar` **passes** (6 theme captures + clean tab state + forced-colors/reduced-motion). It was failing in **five** ways, not one; all restated: the retired-kicker `groupVisible`, `backgroundAlpha >= 0.9` (the strip is transparent canvas by contract), the per-material blur on the bar (none in any theme), `systemKeyline` (no border survives in the register — replaced with `systemField`, that forced colours reach the bar at all), and `pendingScreenshots.length === THEMES.length` (8 vs 6, because two rows carry id `"light"` — now compared by filename, and the clean-state capture is keyed on the row). Also: the gate pressed Enter as an untrusted DOM event, which has no default action — it now uses `Input.dispatchKeyEvent` with `text: "\r"`. New `openInStrip` metric asserts the plus is in the bar, outside tablist and toolbar, and seated in the tab row.
- **(g) Orphaned reopen CSS** — **verified truly unreferenced** (no JSX renders it; `data-study-reopen-recent` is a different element in All Tabs). All 8 rules removed; the premium "sized as one family" regex re-canonned and backed by `doesNotMatch(stylesDeclarations, /scripture-workspace-reopen/)`.

## Commit 2 — `4a6a26c` `fix: the rail stops pretending to switch studies`
4 files, +104/−182. Derivation and markup gone from `app.tsx`, all `.rail-studies*` rules gone from `rail.css` (replaced by a dated retirement note listing the five defects). The pre-existing inert `.rail-study` block is untouched; `.rail-study-line.is-current` stays. Contracts restated: premium's "a study is named in the rail instead" → named by the actions cluster's Manage control and by a folded study's proxy tab (explicitly flagged as a smaller claim than the rail's until the study line lands); rev05-rail-frame's `.rail-studies-list` label-column requirement → `doesNotMatch(railDeclarations, /\.rail-studies/)`. Swept two more stale prose references (premium :293, :606). No `data-study-switch`, `studySwitcher`, or `.rail-studies` remains outside retirement comments.

## Gates
`npm run lint` clean. `npm test` 1464 tests / 1432 pass / **0 fail** / 32 skipped. `npm run qa:study-workspace-bar` PASS after both commits.

## Found but not fixed
1. **The auto end-margin on the plus is inert — the actions cluster is not at the far edge.** Measured in a real Chromium: the Tooltip primitive wraps the plus in `.control-tooltip-anchor` (`display: inline-flex`, sized to content), so the plus is a flex item of *that*, not of the bar; auto margins resolve against free space and an intrinsically-sized container has none. The anchor is 32px wide and the toolbar starts at its right edge with ~570px of empty bar beyond. Three places claimed the opposite; I restated all three rather than changing the layout, since moving the cluster is a visible change to the band the study line is about to rebuild. Confirmed visually in the QA screenshot.
2. **`.scripture-workspace-active-group` is a study label in the strip.** The premium contract's "no element in the strip stands for a study" passes only because its regex names the two retired classes. I narrowed the message to "no element among the TABS" and made the naming claim explicit, but the tension with `ae49372`'s stated rationale (the label was meant to leave the strip; it truncates at 132px) is unresolved — it's the next phase's call.
3. **`actions-cluster-position-contract.test.ts`'s `SELECTION_KEY` still doesn't know `.is-current`**, which `.rail-study-line` uses. Pre-existing hole in the sweep, colour-only today; the test's own comment says an unknown state key "is a hole in the sweep, not a passing test."
4. **`tests/revision-append-recovery.test.ts` is flaky under parallel load** — the 100-way git-lock race failed once in a full run and passes in isolation. Unrelated to these changes.
5. **`flushStart` is not exercised by any used-layout gate** — the QA fixture's active tab is mid-strip, so `data-flush-start` never renders there. The guard change is trivial but the squared corner is unverified in a real window.
