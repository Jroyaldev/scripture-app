# Status

This is the living delivery ledger. The build specification and `AGENTS.md` remain the frozen source of truth for invariants and contracts.

## Maturity Rubric

- **L0 - Absent:** not started.
- **L1 - Proof / gate-complete:** exists and passes a focused gate; mocks or happy-path flows may remain.
- **L2 - Functional:** real implementation on realistic data; edge cases, UX, and operations may still be thin.
- **L3 - Production:** hardened, recoverable, tested, and safe for target users.

## Current Ledger

| Component | Cur | Target | Current read / gap |
|---|---:|---:|---|
| Repo hygiene / snapshot | L2 | L2 | Dirty prototype is being preserved on `codex/prototype-snapshot`; `.DS_Store` noise ignored. |
| Reference / anchor / `bref` model | L2 | L3 | Real core exists; needs broader package/data coverage and regression tests. |
| Electron shell + launch | L2 | L3 | App launches after `npm run rebuild:electron`; `npm start` runs an Electron-runtime preflight that fails visibly with an actionable message on ABI mismatch; `main.ts` also guards direct `electron` invocation with a dialog. |
| Library folder layout | L2 | L3 | Matches contract shape; needs package/version refusal hardening. |
| Notes / anchors / highlights / deterministic Living Margin | L2-L3 | L3 | Highlight range/UX hardening landed 2026-07-09: exact cross-verse character endpoints, contiguous selection semantics, range subtraction that preserves outer pieces, transactional create/erase/recolor/delete Undo with stale-state refusal, grouped whole-blob operations, margin-open word highlighting, active-package WEB/KJV isolation across canvas/margin/AI cache, and visually QA'd SVG washes with muted dark gold, broad exterior caps, restrained nub-free internal steps, outline-free color tessellation, and narrow-reflow coverage. Remaining L3 work is broader long-lived history/restore operations and soak coverage. |
| OpenBible cross-reference graph / study surface | L3 | L3 | Complete 2026-07-15: all 344,799 scored OpenBible edges retained across 66 books, with source ranges and 3,512 non-positive edges preserved in storage; verse queries present a top ten and passage queries aggregate to a top eight while filtering non-positive scores only at presentation. OpenBible CC-BY attribution is separate from note-derived connections. Import Doctor verifies coverage, canonical coordinates, uniqueness, range integrity, license, snapshot, and exact score preservation. The desktop Living Margin has clickable translation previews, quiet warm hover/focus treatment, range navigation, supporting-verse context, and no invented relationship taxonomy. |
| M3 AI plumbing | L2 | L2-L3 | B3 Gates 1-2 landed + Gate 3 contract proven live: real DeepSeek LLM path (`deepseek-v4-flash`, latency classes, JSON mode) AND real local embeddings (EmbeddingGemma-300m ONNX q8, asymmetric prefixes, incremental content-hash sync, model-switch invalidation) now power the semantic margin — `deterministicEmbedding` is test-only. Claim-extraction contract (`claims-v1`, strict validation, mandatory evidence) proven live: 5/5 grounded claims from seeded notes surfaced through the margin query path. **B3.5 landed 2026-07-02: the retrieval decision layer is real.** Hybrid retrieval (paragraph-chunk dense + FTS5/BM25 + scripture-reference evidence, RRF k=60), calibrated floors + score-gap + MMR, show-nothing quality bar, deterministic why-provenance on every card, threads scoped to passage, circular suggested-xrefs replaced with notes-cite-derived ones, claims-v2 (verbatim quote verification, evidence-derived confidence, "grounded in N notes" UI). Magic gate M1-M4 locked by `npm run eval:margin`; verified live in-app over CDP. See `tasks/B3.5-margin-retrieval-quality.md`. Remaining for B3: extraction as background jobs (Gate 3, on claims-v2), budget semantics (Gate 5), rebuild + acceptance (Gate 6). |
| M4 source ingestion | L1-L2 | L2-L3 | PDF import/chunk/locator path passes gate; source shelf/workflow is minimal. |
| M5 plugin broker / manifest / theme | L1 | L3 | Default-deny proof exists; Node `vm` sandbox is not hardened enough for third-party plugins. |
| M6 sync | L1 | L3 | Folder-to-folder proof exists; not a real device/cloud sync UX. |
| Desktop Git RevisionStore | L1-L2 | L3 | Works as adapter proof; needs safer restore UX and operational guardrails. |
| Non-Git snapshot RevisionStore | L1 | L1 | Groundwork for iOS/mobile exists; retention pruning and UI are not implemented. |
| iOS client | L0 | L0 deferred | No iOS project/toolchain setup. Do not start before Tier B/C maturity improves. |
| Scripture data WEB/KJV (+ YLT) | L2→L3 | L3 | Full WEB + KJV + YLT across all 66 books (1189 chapters each). **Fixed 2026-07-14:** backbone had short verse counts on 1SA 23, JOB 41–42, 1CO 16 (28/26/12/10 vs classic KJV 29/34/17/24); `build-scripture-packages.mjs` truncated package JSON to those counts, so the reader actually omitted the tail of those chapters — not “counts only.” Backbone restored to classic KJV total **31,102**; WEB/KJV rebuilt for the three books; YLT import Doctor clean (0 backbone Δ). Regression: `tests/scripture-packages.test.ts` hot-chapter + total checks. Package manifests + license flags + formatVersion + Doctor content checks remain. |
| App UX / onboarding / settings / error states | L1-L2 | L2-L3 | B2 in progress: full read-screen redesign landed (collapsible sidebar + library popover, passage/version picker popovers, gradient-wash verse highlights with pinned-passage model, Living Margin 3-state incl. ambient IntersectionObserver reading + per-verse AI insight cache, dark mode + electron-store-persisted settings), live-QA'd via CDP against the running app with real bugs found and fixed (settings self-revert race, Popover viewport clamping, dark-mode toast contrast, stale cross-chapter verse selection). **Language-card orbit visual/data pass landed 2026-07-14:** real gloss band labels, compact reverse hub + scoped count, short header glosses, readable legend/second-activation affordance, singleton reverse-noise filtering, and malformed lexicon refusal. Senses is now source-shaped: nested top-level BDB for Hebrew; occurrence-tagged MACULA/MARBLE semantic outlines for Greek, with frequency rank, a quiet `here` marker, six-row cap, one-sense suppression, and full-label expansion. Thayer prose remains in Definition. A corpus guard scans all 137,779 Greek tokens for clean stable lemma+sense labels, replacing verse-by-verse corrections. **Structure truth landed 2026-07-14:** a real XML parser regenerated 23,213 Hebrew sentences / 474,205 clean leaves; exact contextual focus resolves 98.34% of OSHB words and unresolved cases now refuse instead of guessing. Embedded-clause glosses no longer leak; visible roles are grammatical; Clause is the primary source-order view; Sentence map is conditional on multiple clauses. **Spatial Phrase diagram landed 2026-07-14:** genuinely branched groups now expose local `Diagram | Outline` views. Diagram is a measured parent-over-children constituency graph with aligned terminals, RTL mirroring, selected ancestry, source-order branches, and solid/direct versus dashed/condensed provenance; Outline remains the exhaustive fallback. Wide, deep, and genealogy shapes use explicit focus projections instead of tiny text, while embedded-clause terminals navigate to Clause. A 44,000-detail shipped-corpus probe retained exact focus and bounded fan-out across 42,769 complete diagrams and 1,231 outline-first exceptions. Romans 1:4's passive `ὁρίζω` complement is now grammatically labeled **Predicate complement**. **Continuous Structure navigation landed 2026-07-14:** Clause now exposes direct Previous/Next movement with previews and return-to-selection; Phrase detail steps through only real branched phrases across clause boundaries, ordered by actual source-word position. Position context, explicit boundaries, `Option+Left/Right`, view-choice persistence, and focus restoration remove Sentence map as a required detour. **Structure visual refinement landed 2026-07-14:** role-color edges and labels, accent rails, selected-path color, and accent shadows were replaced by one neutral hierarchy across Clause, Diagram, Outline, Sentence map, navigation, and modal chrome. The exact selected source word retains the sole semantic accent; keyboard focus uses a visible neutral ring. Final desktop Greek/Hebrew light/dark captures are under `docs/ui-audit/structure/structure-minimal-*`. **Desktop visual foundation landed 2026-07-15:** Paper, Ink, Glass, and Candlelight now share a warm token system, one gold action/focus language, portal-safe floating layers, an atmosphere picker, suite-owned glass backgrounds, and all-look Electron QA. **Sidebar and primary navigation landed 2026-07-15:** the layout lab and its stored modes are gone; production density is 228px expanded / 64px collapsed. Product identity is static, the footer is the one library switcher, current/hover/focus states are restrained and accessible, 1–5 hints reveal contextually, and status truthfully distinguishes local idle state from source-backed study work. `npm run qa:sidebar` covers all four atmospheres and the interaction states. The continuation sequence is fixed in `docs/desktop-visual-system.md`; Reading topbar is next. Mobile stays in its separate worktree. Onboarding/library-picker first-run flow and source/sync surfaces still need deliberate work — biggest remaining B2 gap. |
| CI / native rebuild / lint | L2 | L2 | Two explicit commands (`rebuild:node` / `rebuild:electron`) cover the ABI 137/133 split; `npm run lint` reproducible (exit 0); `npm test` ABI-independent (8/8); preflight gates Electron launch. See `docs/native-build.md`. CI matrix not yet configured. |

## Sequenced Backlog

1. **A0 Snapshot current prototype:** preserve the dirty tree on a branch, ignore noise, commit non-noise work, and document current state.
2. **A1 Stabilize native build and lint:** make Electron and Node-side verification reproducible without manual ABI rebuilds. — **DONE** (see `docs/native-build.md`; `rebuild:node`/`rebuild:electron` + Electron preflight).
3. **B1 Full scripture package path:** promote WEB/KJV data from partial demo to real package coverage and refusal behavior. — **DONE** (1189 chapters × 2 translations; manifests + version refusal + Doctor content checks).
4. **B2 App UX pass:** onboarding, library picker, loading/error states, settings, source/import screens, sync status.
5. **B3 Real AI and embeddings:** BYOK/local provider flow, real embeddings, queue/retry/error handling. — **IN PROGRESS** (Gates 1-2 of 6 done; Gates 3-6 paused pending B3.5; staged plan in `tasks/B3-real-ai-and-embeddings.md`).
6. **B3.5 Margin retrieval quality ("magic gate"):** hybrid retrieval, calibrated quality bar, deterministic provenance, grounded claims, eval harness — **DONE** (2026-07-02; `npm run eval:margin` locks M1-M4; see `tasks/B3.5-margin-retrieval-quality.md`).
7. **B3.6 Quick-note magic:** capture-time note enrichment (inferred refs, themes, expansion; confirm-to-promote) so ref-free quick notes resurface — **NEXT** (persona study 2026-07-02 measured 0/13 expected resurfacings for realistic note styles; `npm run eval:personas`; plan in `tasks/B3.6-quick-note-magic.md`; signal map in `docs/intelligence-layer.md`).
8. **B4 Production rebuild story:** delete `.system/` and rebuild source chunks/indexes/embeddings deterministically.
9. **C1 Revision safety:** safe restore UX and data-loss guardrails.
10. **C2 Real sync UX:** provider choice, device identity, conflict UI, progress, license enforcement.
11. **C3 Plugin isolation L3:** process isolation or equivalent before third-party plugins.
12. **D1 iOS:** deferred until the desktop daily-use path is substantially real.
